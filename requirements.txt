# =============================================================================
# SOTA Video Enhancer Requirements (2025)
# Optimized for VSRM, SeedVR2, DiTVR, Fast Mamba VSR deployment
# =============================================================================

# Core Dependencies
numpy>=1.24.0
scipy>=1.11.0
pillow>=10.0.0
opencv-python>=4.8.1
ffmpeg-python>=0.2.0
requests>=2.31.0

# PyTorch & AI Core (SOTA Requirements)
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.36.0
diffusers>=0.25.0
accelerate>=0.25.0
safetensors>=0.4.1
huggingface-hub>=0.20.0

# SOTA Model Backbones
# 1. Mamba State-Space Models (VSRM, Fast Mamba VSR)
mamba-ssm>=1.2.0
causal-conv1d>=1.1.0
einops>=0.7.0

# 2. Diffusion Models (SeedVR2, DiTVR)
diffusers>=0.25.0
compel>=2.0.0

# 3. Vision Transformers (DiTVR)
timm>=0.9.16

# 4. Advanced Convolutions
triton>=2.1.0; sys_platform == "linux"

# Video Processing & Computer Vision
imageio>=2.33.0
imageio-ffmpeg>=0.4.9
scikit-image>=0.22.0

# Face Restoration (SOTA Enhancement)
gfpgan>=1.3.8
basicsr>=1.4.2
facexlib>=0.3.0
realesrgan>=0.3.0
insightface>=0.7.3
onnxruntime>=1.16.0

# Frame Interpolation (RIFE)
# For high frame rate output
torch-sparse>=0.6.18; sys_platform == "linux"

# Quality Assessment & Metrics
lpips>=0.1.4
torchmetrics>=1.2.0

# Gradio Web Interface
gradio>=4.44.0
fastapi>=0.105.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# Flask for Health/Metrics Endpoints
flask>=2.3.0
werkzeug>=2.3.0

# FastAPI and Prometheus for Advanced Monitoring
fastapi>=0.105.0
prometheus-client>=0.19.0

# Intelligent Degradation Routing
scikit-learn>=1.3.2

# Utilities & Performance
tqdm>=4.66.0
rich>=13.7.0
pyyaml>=6.0.1
psutil>=5.9.6
jsonschema>=4.20.0

# GPU Optimization (Production)
xformers>=0.0.23

# Development Tools (Optional)
# Uncomment for development
# pytest>=7.4.3
# black>=23.12.0
# flake8>=7.0.0

# =============================================================================
# DEPLOYMENT PROFILES
# =============================================================================

# For CPU-only deployment (remove GPU-specific packages):
# pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu

# For CUDA 12.1 deployment:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# pip install -r requirements.txt

# For Apple Silicon (MPS) deployment:
# All packages compatible with Apple Silicon M1/M2/M3
