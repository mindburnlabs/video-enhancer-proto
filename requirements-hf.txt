# Minimal requirements for HuggingFace Spaces deployment
# Core scientific stack
numpy>=1.24.0
scipy>=1.11.0
pillow>=10.0.0
opencv-python>=4.8.1
ffmpeg-python>=0.2.0
requests>=2.31.0

# PyTorch (CPU-only for Spaces)
torch>=2.1.0,<2.5.0
torchvision>=0.16.0,<0.20.0

# HuggingFace ecosystem
transformers>=4.36.0
diffusers>=0.25.0
accelerate>=0.25.0
safetensors>=0.4.1
huggingface-hub>=0.20.0

# Web interface
gradio>=4.0.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
imageio>=2.31.1
imageio-ffmpeg>=0.4.8

# FastAPI and Prometheus for Advanced Monitoring
fastapi>=0.105.0
prometheus-client>=0.19.0

# Face restoration (basic CPU versions)
basicsr>=1.4.2
gfpgan>=1.3.8
realesrgan>=0.3.0

# Exclude problematic dependencies:
# - mamba-ssm (requires NVCC)
# - xformers (GPU optimization)
# - flash-attn (GPU optimization)